{
  "name": "twenty-dashboard",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/twenty-dashboard",
  "projectType": "application",
  "tags": ["scope:dashboard"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/.next"],
      "options": {
        "cwd": "{projectRoot}",
        "command": "npx next build"
      },
      "dependsOn": ["^build"]
    },
    "dev": {
      "executor": "nx:run-commands",
      "cache": false,
      "options": {
        "cwd": "{projectRoot}",
        "command": "npx next dev"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "cache": false,
      "dependsOn": ["build"],
      "options": {
        "cwd": "{projectRoot}",
        "command": "npx next start"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "npx next lint"
      }
    },
    "lint:diff-with-main": {},
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "npx tsc --noEmit"
      }
    },
    "fmt": {
      "options": {
        "files": "."
      },
      "configurations": {
        "fix": {}
      }
    }
  }
}
